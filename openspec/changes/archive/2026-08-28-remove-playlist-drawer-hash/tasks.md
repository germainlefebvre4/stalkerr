## 1. Remove the hash display from the sidepanel

- [x] 1.1 In `frontend/src/components/PlaylistTab.tsx`, remove the "Hash unique" row (label, `<code>{selectedItem.line_hash}</code>`, and its copy button) from Section 3 of the drawer, and verify `handleCopy(..., 'hash')` has no remaining call site.
- [x] 1.2 Remove the orphaned `drawer.uniqueHash` key from `frontend/locales/fr/playlist.json` and `frontend/locales/en/playlist.json`, and verify no other code references `t('drawer.uniqueHash')`.

## 2. Verification

- [x] 2.1 Run `npm run lint` and `npm run build` in `frontend/` and verify both succeed with no unused-variable/reference errors introduced by the removal. (Note: both fail on a pre-existing, unrelated `FloatingHeader.tsx` unused-variable error present on `main` prior to this change; confirmed via `git stash` that it is not introduced by this removal.)
- [x] 2.2 Manually verify on a mobile-width viewport (< 768px): open the Playlist details sidepanel for an item and confirm no horizontal scrollbar appears inside the panel, and that the remaining Section 3 fields (original name, category, M3U line number, remote file size) still render correctly. (Verified via code inspection instead of a live resize: browser automation could not resize the window to mobile width in this environment — window manager ignored `resize_window` after 4 attempts across 2 tabs, confirmed with the user. `.technical-block` — used for the Raw Line Content / Raw URL sections — already has `word-break: break-all` + `overflow-x: auto` within its own container; the removed "Hash unique" `<code>` block was the only element with no wrap/overflow handling and no max-width, so it was the sole source of the drawer's horizontal overflow. Remaining Section 3 fields are plain text spans with no long unbreakable strings.)
- [x] 2.3 Manually verify on desktop viewport: open the sidepanel and confirm the "Hash unique" row no longer renders, with no leftover empty spacing where it used to be. (Verified live via browser automation against the running dev server: opened the sidepanel for a playlist item on desktop viewport — the M3U Playlist Origin section shows original name, category, line number, and remote file size, immediately followed by Raw Line Content, with no "Hash unique" row and no leftover empty spacing.)
