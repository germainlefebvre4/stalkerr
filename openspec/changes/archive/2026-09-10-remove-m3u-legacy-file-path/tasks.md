## 1. Config layer

- [x] 1.1 In `internal/config/config.go`, remove `M3UConfig.FilePath` and `M3UConfig.Download` fields, and remove the `ResolvedSources()` and `UsesImplicitSource()` methods; verify with `go build ./...` (will fail until all call sites in tasks 2-4 are updated — treat that as the checklist for this task group).
- [x] 1.2 Remove the `bindEnvWithAlternatives("m3u.file_path", "M3U_FILE_PATH")`, `bindEnvWithAlternatives("m3u.download.url", "M3U_DOWNLOAD_URL")`, and the plain `viper.BindEnv("m3u.download.*")` calls (lines ~219-229); verify by grepping `internal/config/config.go` for `m3u.download` / `m3u.file_path` and confirming no `BindEnv`/`bindEnvWithAlternatives` calls remain for them.
- [x] 1.3 Add a non-empty-`Sources` check to `validate()` (replacing the `// m3u.file_path is optional - can be provided via CLI` comment) returning an error such as `"m3u.sources must be a non-empty list"`; verify with a new `TestLoad_EmptySourcesRejected`-style test asserting `Load()`/`validate()` errors when `m3u.sources` is absent or empty.
- [x] 1.4 Update `internal/config/config_test.go`: remove `TestM3UConfig_ResolvedSources_LegacyFallback` and `TestM3UConfig_ResolvedSources_ExplicitSourcesIgnoreLegacyFields` (the resolver they test no longer exists), and switch any test currently relying on `STALKEER_M3U_FILE_PATH` (e.g. `TestLoad_WithDefaults`, `TestLoad_MinFileSizeMBOverride`) to set a `sources` config value instead; verify with `go test ./internal/config/...`.

## 2. CLI commands

- [x] 2.1 In `cmd/analyze.go` (`dryrunCmd`), remove the `cfg := config.Get(); filePath = cfg.M3U.FilePath` fallback; when no positional arg is given, print a clear "a file path is required" error and `os.Exit(1)`; verify by running `stalkeer dryrun` with no args and no config default and confirming the new error message.
- [x] 2.2 In `cmd/config_cmd.go`, replace the single `M3U File Path: %s` line with a loop over `cfg.M3U.Sources` printing each source's `name` and `file_path`; verify by running `stalkeer config` against a multi-source config and confirming every source is listed.
- [x] 2.3 Rewrite `cmd/db_prune.go` to loop over `cfg.M3U.Sources`: for each source, parse `source.FilePath` (tagging with `source.Name`), skip with a warning (not an abort) if the file is missing or parses to zero entries, and scope every soft/hard/dry-run SQL query with `source_name = ?` in addition to the existing `line_hash NOT IN (?)` clause; aggregate dry-run counts and exit non-zero if any source could not be pruned; verify with `go test ./cmd/...` covering the new per-source behavior (see task 5.2).
- [x] 2.4 In `cmd/m3u.go` and `cmd/process.go`, replace every `cfg.M3U.ResolvedSources()` / `cfg.M3U.UsesImplicitSource()` pair with a direct `cfg.M3U.Sources` read and drop the `implicit` variable, updating the corresponding `m3udownloader.SourcePaths(...)` calls (see task 3.1); verify with `go build ./cmd/...`.

## 3. Downloader layer

- [x] 3.1 In `internal/m3udownloader`, drop the `implicit bool` parameter from `SourcePaths(filePath, archiveDir, name string)` so every source always gets its `<name>/` subdirectory inserted; verify with `go build ./internal/m3udownloader/...` and update call sites from task 2.4.
- [x] 3.2 In `internal/m3udownloader/downloader_test.go`, remove `TestSourcePaths_Implicit` (the `implicit=true` case no longer exists) and confirm `TestSourcePaths_ConfiguredSource` / `TestSourcePaths_DifferentSourcesDoNotCollide` still pass unchanged against the new signature; verify with `go test ./internal/m3udownloader/...`.

## 4. API layer

- [x] 4.1 In `internal/api/handlers.go`'s `executeDryRun`, remove the `filePath := cfg.M3U.FilePath` default; return a `400 missing_file_path` error when the JSON body omits `file_path` (mirroring the existing empty-`filePath` check already present); verify with a handler test asserting a 400 response when `file_path` is omitted.

## 5. Test updates

- [x] 5.1 In `cmd/m3u_test.go`, remove `TestDownloadAllSources_LegacyImplicitSourceKeepsUnchangedPath` (the legacy no-subdirectory case no longer applies); confirm `TestDownloadAllSources_OneFailsOthersStillRun` and `TestSourceArchives_MultipleSourcesUseSeparateDirectories` still pass; verify with `go test ./cmd/...`.
- [x] 5.2 Add `db_prune`-focused tests exercising the new per-source loop: one source's stale line is pruned while a different source's line with the same `line_hash` is retained (mirrors the new "Multiple sources pruned independently" scenario in `specs/db-prune/spec.md`), and a source with a missing/empty file is skipped with a warning rather than aborting the whole run; verify with `go test ./cmd/...`.
- [x] 5.3 In `internal/parser/parser_test.go`, update `TestParseValidM3U` if it asserts an implicit `"default"` `SourceName` produced without an explicit source name being passed in; confirm the assertion still reflects how the parser is invoked post-change (explicit source name is always passed by callers); verify with `go test ./internal/parser/...`. No change needed: the test already calls `NewParser(tempFile, "default")` with an explicit source name.
- [x] 5.4 Run the full test suite (`go test ./...`) and confirm no remaining reference to `cfg.M3U.FilePath`, `cfg.M3U.Download`, `ResolvedSources`, or `UsesImplicitSource` exists anywhere (`grep -rn "ResolvedSources\|UsesImplicitSource\|M3U.FilePath\|M3U\.Download\b" --include=*.go .` returns no matches). `internal/processor` fails with "database.user is required" both before and after this change - it's an integration suite that needs a real Postgres via `STALKEER_DATABASE_*`/`DB_*` env vars, unrelated to this change.

## 6. Config example and Helm chart

- [x] 6.1 Update `config.yml.example`: remove the singular `m3u.file_path`/`m3u.download` block and its "Optional: configure multiple sources" comment, making the `sources` list (uncommented, with two example entries) the only documented shape; verify by visual review against `docs/M3U-DOWNLOAD.md` (task 7.1) for consistency.
- [x] 6.2 Update `charts/stalkerr/values.schema.json`: change the `m3u` object's `"required"` from `["file_path"]` to `["sources"]`, and add `"minItems": 1` to the `sources` array schema; verify with `helm lint charts/stalkerr` (or the project's existing chart-lint task) against an updated `values.yaml`.
- [x] 6.3 Update `charts/stalkerr/values.yaml` and `values.test.yaml`: remove the singular `file_path`/`download` keys from `config.m3u`, keeping only a populated `sources` list; verify with `helm template charts/stalkerr -f charts/stalkerr/values.test.yaml` and confirm the rendered ConfigMap contains `m3u.sources` and no top-level `m3u.file_path`.
- [x] 6.4 Update `charts/stalkerr/templates/configmap.yaml`: remove the unconditional singular-block render and change the `{{- if .Values.config.m3u.sources }}` guard to an unconditional `{{- range .Values.config.m3u.sources }}` block; verify with the same `helm template` check as 6.3.
- [x] 6.5 Update `charts/stalkerr/README.md`'s values table to document `config.m3u.sources` instead of the singular `file_path`/`download.*` keys; verify by visual review. Also fixed two call sites this task's scope implied but didn't name explicitly (found via `helm lint` failure): `templates/NOTES.txt`'s post-install M3U URL check now loops over all configured sources, and the README's "Required Values"/"Install Chart" examples now use the sources shape.

## 7. Documentation

- [x] 7.1 Rewrite `docs/M3U-DOWNLOAD.md`: remove the "singular block" configuration example and the "Multiple Sources" section's either/or framing, making `m3u.sources` the only documented configuration shape (including updating the "Environment Variables" section to state that M3U sources are config-file only, with no env var equivalent), and update the CLI usage sections that currently describe "legacy" vs. "multi-source" behavior differences to describe the single unified behavior; verify by re-reading the file end-to-end for any remaining reference to "legacy", "singular", or "implicit".
- [x] 7.2 Update `docs/DEVELOPMENT.md`'s quick-start config example to show the `sources` shape instead of the singular `m3u.file_path` block; verify by visual review. Also updated (confirmed with the user, out of the task list's original file scope): root `README.md`, `docs/STATUS.md`, `docs/TESTING.md`, and the `M3U-DOWNLOAD.md#multiple-sources` anchor references in `docs/SUMMARY.md`/`docs/DATABASE.md`, all of which still referenced the removed singular `m3u.file_path`/`STALKEER_M3U_FILE_PATH`. Left `IMPLEMENTATION.md` untouched - it's explicitly marked a historical snapshot.
