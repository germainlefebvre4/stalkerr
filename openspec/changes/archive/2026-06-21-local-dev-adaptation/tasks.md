## 1. Makefile Modifications

- [x] 1.1 Add frontend make targets (`front-install`, `front-dev`, `front-build`, `front-lint`) to root `Makefile`
- [x] 1.2 Add unified hybrid development `make dev` target to root `Makefile`
- [x] 1.3 Add frontend target details inside the `make help` description block of `Makefile`

## 2. README.md Documentation

- [x] 2.1 Update root `README.md` to introduce the new Web Dashboard and detail its features (playlist, processing logs, downloads progress, directory re-organizations)
- [x] 2.2 Update root `README.md` with step-by-step local development instructions (prerequisites, setup, running `make dev`, default ports)

## 3. Local Verification

- [x] 3.1 Validate Makefile targets and verify README formatting
