## 1. Schema Integration

- [x] 1.1 Add the `frontend` validation block to `charts/stalkerr/values.schema.json`

## 2. Validation & Documentation

- [x] 2.1 Verify Helm chart template rendering and validation with default values using `helm template`
- [x] 2.2 Verify Helm chart template validation fails when invalid values are provided (e.g. non-boolean `enabled`, invalid replicaCount)
- [x] 2.3 Update the global specification file `openspec/specs/helm-chart-frontend-integration/spec.md` with the new requirements
