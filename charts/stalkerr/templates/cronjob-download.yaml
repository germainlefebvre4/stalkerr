{{- if .Values.jobs.download.enabled }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "stalkerr.fullname" . }}-download
  labels:
    {{- include "stalkerr.labels" . | nindent 4 }}
    app.kubernetes.io/component: cronjob-download
spec:
  timeZone: {{ .Values.jobs.download.timeZone | quote }}
  schedule: {{ .Values.jobs.download.schedule | quote }}
  concurrencyPolicy: {{ .Values.jobs.download.concurrencyPolicy }}
  successfulJobsHistoryLimit: {{ .Values.jobs.download.successfulJobsHistoryLimit }}
  failedJobsHistoryLimit: {{ .Values.jobs.download.failedJobsHistoryLimit }}
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            {{- include "stalkerr.selectorLabels" . | nindent 12 }}
            app.kubernetes.io/component: cronjob-download
        spec:
          {{- with .Values.imagePullSecrets }}
          imagePullSecrets:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          serviceAccountName: {{ include "stalkerr.serviceAccountName" . }}
          securityContext:
            {{- toYaml .Values.podSecurityContext | nindent 12 }}
          restartPolicy: {{ .Values.jobs.download.restartPolicy }}
          containers:
            - name: download
              securityContext:
                {{- toYaml .Values.securityContext | nindent 16 }}
              image: {{ include "stalkerr.image" . }}
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              command:
                - ./stalkeer
                - download
                - --config
                - /app/config/config.yml
              env:
                - name: STALKERR_DATABASE_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: {{ include "stalkerr.secretName" . }}
                      key: database-password
                - name: DATABASE_URL
                  value: {{ include "stalkerr.databaseUrl" . | quote }}
                - name: RADARR_API_KEY
                  valueFrom:
                    secretKeyRef:
                      name: {{ include "stalkerr.secretName" . }}
                      key: radarr-api-key
                - name: SONARR_API_KEY
                  valueFrom:
                    secretKeyRef:
                      name: {{ include "stalkerr.secretName" . }}
                      key: sonarr-api-key
              resources:
                {{- toYaml .Values.jobs.download.resources | nindent 16 }}
              volumeMounts:
                - name: config
                  mountPath: /app/config/config.yml
                  subPath: config.yaml
                  readOnly: true
                - name: m3u-storage
                  mountPath: /data/m3u
                - name: media-storage
                  mountPath: /media
                - name: temp-storage
                  mountPath: /tmp
          volumes:
            - name: config
              configMap:
                name: {{ include "stalkerr.configMapName" . }}
            - name: m3u-storage
              persistentVolumeClaim:
                claimName: {{ include "stalkerr.m3uPvcName" . }}
            - name: media-storage
              persistentVolumeClaim:
                claimName: {{ include "stalkerr.mediaPvcName" . }}
            - name: temp-storage
              emptyDir: {}
{{- end }}
